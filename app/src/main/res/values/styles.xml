<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorSecondary">@color/primaryColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceTheme</item>
        <item name="android:windowLightStatusBar">@bool/lightStatusBar</item>
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
    </style>

    <style name="AppTheme.Caption" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="PreferenceTheme" parent="@style/PreferenceThemeOverlay">
        <!--<item name="android:background">@color/white</item>-->
        <item name="android:divider">@null</item>
        <item name="android:dividerHeight">0dp</item>
        <item name="background">?attr/colorSurface</item>
        <item name="switchPreferenceCompatStyle">@style/PreferenceTheme.Switch</item>
    </style>

    <style name="PreferenceTheme.Switch" parent="@style/Preference.SwitchPreferenceCompat.Material">
        <item name="android:listPreferredItemPaddingStart">0dp</item>
        <item name="android:listPreferredItemPaddingEnd">0dp</item>
    </style>
</resources>
